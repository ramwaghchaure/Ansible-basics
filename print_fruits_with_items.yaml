---
-  name: 'Print list of fruits'
   hosts: localhost
   vars:
      fruits:
        - Apple
        - banana
        - Grapes
        - orange

    tasks:
      - command: 'echo "{{ item }}"'
        with_items: '{{ fruits }}'
