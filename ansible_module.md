###  list of ansible modules and their use cases
#### 1. command :
excute command on remote host. parmater of command module listed in ansible documentation page.
#### 2. script:
run a local script on a remote node after transferring it

